[package]
name = "examples-app"
version = "0.1.0"
edition = "2021"

# Pulls the engine from crates.io (NOT the local repo), to demonstrate the
# published crate end-to-end.
[dependencies]
async_jsonata_rust = "0.3.0"
futures = "0.3"
serde_json = "1"
